# انجام همه‌چیز فقط با گوشی

ریپوی تو آماده است: **github.com/parvaneshahraki978-coder/Onvo**

سه کار مانده. هر سه از مرورگر گوشی انجام می‌شوند و هیچ برنامه‌ای لازم نیست.
زمان کل: حدود ۱۵ دقیقه.

---

# مرحلهٔ ۱ — آپلود سورس ▸ ۵ دقیقه

## چرا نمی‌شود مستقیم ZIP را آپلود کرد

گیت‌هاب فایل ZIP را باز نمی‌کند؛ آن را مثل یک فایل معمولی نگه می‌دارد.
پس باید فایل‌ها را از حالت فشرده خارج کنی و بعد آپلود کنی.

## کاری که باید بکنی

**۱.** فایل `onvo-source.zip` را از همین گفتگو دانلود کن.

**۲.** در برنامهٔ **Files** گوشی (یا هر فایل‌منیجری) روی آن بزن و
**Extract / استخراج** را انتخاب کن. یک پوشه با ۵۱ فایل ساخته می‌شود.

**۳.** در مرورگر گوشی برو به:

```
github.com/parvaneshahraki978-coder/Onvo/upload/main
```

**۴.** روی **choose your files** بزن، وارد پوشهٔ استخراج‌شده شو و
**همهٔ محتوایش را انتخاب کن**.

**۵.** پایین صفحه در کادر پیام بنویس `Onvo 0.3.0` و **Commit changes** را بزن.

## ⚠️ نکتهٔ حیاتی: پوشهٔ `.github`

آپلودکنندهٔ وب گیت‌هاب گاهی پوشه‌هایی که با نقطه شروع می‌شوند را نادیده
می‌گیرد. **بدون این پوشه هیچ APK ساخته نمی‌شود**، چون دستور ساخت داخل آن است.

بعد از آپلود، صفحهٔ ریپو را باز کن و ببین `.github` هست یا نه.

**اگر نبود، این‌طور دستی بسازش:**

۱. برو به:

```
github.com/parvaneshahraki978-coder/Onvo/new/main
```

۲. در کادر نام فایل، دقیقاً این را بنویس — با هر `/` یک پوشه ساخته می‌شود:

```
.github/workflows/build.yml
```

۳. فایل `build.yml` را از پوشهٔ استخراج‌شده با یک ویرایشگر متن باز کن،
کل محتوایش را کپی کن و در کادر بزرگ پیست کن.

۴. **Commit changes**.

---

# مرحلهٔ ۲ — کلید امضا ▸ ۵ دقیقه

بدون این هم APK ساخته می‌شود، ولی فقط نسخهٔ debug. با این، نسخهٔ release
با R8 و بهینه‌سازی کامل هم می‌آید.

## چرا این کلید مهم است

اندروید نصب آپدیت با امضای متفاوت را **رد می‌کند**. اگر این کلید گم شود،
دیگر هرگز نمی‌توانی برای کسانی که اپ را نصب کرده‌اند آپدیت بدهی — مجبور
می‌شوی از همه بخواهی اپ را پاک و دوباره نصب کنند.

**پس حتماً از فایل کلید پشتیبان بگیر.**

## ساخت کلید روی گوشی

از گوشی نمی‌شود مستقیم `keytool` اجرا کرد، ولی گیت‌هاب می‌تواند این کار
را برایت انجام دهد.

**۱.** یک فایل موقت بساز:

```
github.com/parvaneshahraki978-coder/Onvo/new/main
```

نام فایل:

```
.github/workflows/make-key.yml
```

محتوا (این را کامل کپی کن):

```yaml
name: Make signing key
on: workflow_dispatch
jobs:
  key:
    runs-on: ubuntu-latest
    steps:
      - name: Generate keystore
        run: |
          keytool -genkeypair -v \
            -keystore onvo-release.jks \
            -alias onvo \
            -keyalg RSA -keysize 4096 -validity 10000 \
            -storepass "${{ github.run_id }}${{ github.run_number }}Onvo!" \
            -keypass  "${{ github.run_id }}${{ github.run_number }}Onvo!" \
            -dname "CN=Onvo, O=Onvo, C=DE"
          base64 -w0 onvo-release.jks > keystore.b64
          echo "::notice::PASSWORD IS ${{ github.run_id }}${{ github.run_number }}Onvo!"
      - uses: actions/upload-artifact@v4
        with:
          name: signing-key
          path: |
            onvo-release.jks
            keystore.b64
```

**۲.** Commit کن، بعد برو به تب **Actions** ← از لیست چپ **Make signing key**
را انتخاب کن ← دکمهٔ **Run workflow** ← دوباره **Run workflow**.

**۳.** بعد از حدود یک دقیقه، روی اجرای انجام‌شده بزن. رمز عبور را در
بخش **Annotations** بالای صفحه می‌بینی. آن را **همین حالا یادداشت کن**.

**۴.** پایین همان صفحه، **Artifacts ← signing-key** را دانلود کن و
جای امنی نگه دار. این پشتیبان توست.

**۵.** فایل ZIP دانلودشده را استخراج کن و `keystore.b64` را با یک
ویرایشگر متن باز کن. کل محتوایش را کپی کن — یک رشتهٔ خیلی طولانی است.

**۶.** برو به:

```
github.com/parvaneshahraki978-coder/Onvo/settings/secrets/actions
```

با **New repository secret** چهار مورد بساز:

| Name | Secret |
|---|---|
| `KEYSTORE_B64` | محتوای `keystore.b64` |
| `STORE_PASSWORD` | رمزی که یادداشت کردی |
| `KEY_ALIAS` | `onvo` |
| `KEY_PASSWORD` | همان رمز |

**۷.** حالا فایل `make-key.yml` را **پاک کن** — کارش تمام شده و ماندنش
بی‌دلیل است. در صفحهٔ آن فایل، منوی سه‌نقطه ← **Delete file** ← Commit.

---

# مرحلهٔ ۳ — گرفتن APK ▸ ۳۰ دقیقه انتظار

اگر مرحلهٔ ۱ را درست انجام داده باشی، ساخت **خودکار** شروع شده است.

برو به تب **Actions**. یک اجرا با نام `Build Onvo` می‌بینی.

## چه اتفاقی می‌افتد

| بخش | کار | زمان |
|---|---|---|
| `native` × ۳ | کامپایل Aether (Rust) و ByeDPI (C) با NDK | ۲۰ تا ۲۵ دقیقه |
| `apk` | ساخت APK با هسته‌های واقعی | ۵ دقیقه |

کامپایل Rust کند است، مخصوصاً quiche. صبور باش.

## دریافت فایل

وقتی تیک سبز خورد: روی اجرا بزن ← پایین صفحه **Artifacts** ←
**onvo-apks** را دانلود کن.

داخلش چند APK هست. اگر مطمئن نیستی کدام، **universal** را نصب کن.

## انتشار رسمی (اختیاری)

اگر می‌خواهی یک صفحهٔ Release با لینک دائمی داشته باشی:

```
github.com/parvaneshahraki978-coder/Onvo/releases/new
```

در کادر تگ بنویس `v0.3.0` ← **Create new tag** ← **Publish release**.

APKها و فایل چک‌سام خودکار به آن اضافه می‌شوند. بروزرسانی داخل‌برنامه
بعداً از همین‌جا می‌خواند.

---

# اگر ساخت قرمز شد

روی اجرای قرمز بزن و ببین کدام بخش شکسته:

| نشانه | علت | راه‌حل |
|---|---|---|
| هیچ اجرایی شروع نشد | پوشهٔ `.github` آپلود نشده | مرحلهٔ ۱، بخش هشدار |
| `native` قرمز شد | ریپوی بالادست تغییر کرده | متن خطا را برایم بفرست |
| `apk` قرمز ولی `native` سبز | معمولاً مشکل امضا | مرحلهٔ ۲ را دوباره چک کن |
| فقط debug ساخته شد | secretها تعریف نشده‌اند | طبیعی است، APK کار می‌کند |

متن خطا را کپی کن و برایم بفرست؛ درستش می‌کنم.

---

# بعد از نصب

اپ را باز کن و اجازهٔ VPN را بده. اگر گوشیت **شیائومی** است، حتماً به
**تنظیمات ← سازگاری با گوشی** برو و هر چهار مورد را فعال کن. بدون
اجازهٔ اجرای خودکار، MIUI اتصال را هنگام خاموش بودن صفحه قطع می‌کند.

اگر وصل نشد، **منو ← کنسول و لاگ** را باز کن، سوییچ «نمایش جزئیات فنی»
را روشن کن و **کپی همه** را بزن. کلیدها و آی‌پی خودت موقع کپی حذف
می‌شوند، پس می‌توانی با خیال راحت برایم بفرستی.
