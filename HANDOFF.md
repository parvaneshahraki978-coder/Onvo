# Onvo — بریفینگ انتقال به سشن جدید

> این فایل برای کسی نوشته شده که هیچ‌چیز از گفتگوی قبلی نمی‌داند.
> همه‌چیز لازم اینجاست. گفتگوی قبلی را نمی‌خواهی.

**ریپو:** `github.com/parvaneshahraki978-coder/Onvo`
**نسخه:** 0.4.0
**لایسنس:** AGPL-3.0 (اجباری، چون Aether همین لایسنس را دارد)

---

## کاری که باید انجام شود

سورس کامل و کامپایل‌شونده است. یک کار مانده:

> **سورس را در ریپو push کن، ساخت را اجرا کن، APK کارکن بگیر.**

کاربر به گیت‌هاب دسترسی دارد و توکن می‌سازد. نوت‌بوک Colab
(`tools/Onvo_Setup.ipynb`) این کار را خودکار انجام می‌دهد، یا با
`git push` معمولی هم می‌شود.

**نکته:** اگر کلید امضا از قبل در secretهای ریپو هست، سلول ۵ نوت‌بوک را
اجرا نکن — کلید نباید عوض شود، وگرنه کاربران فعلی نمی‌توانند آپدیت بگیرند.

---

## اپ چیست

کلاینت عبور از سانسور برای اندروید، ساخته‌شده روی دو پروژهٔ موجود:

| منبع | نقش | لایسنس |
|---|---|---|
| [CluvexStudio/Aether](https://github.com/CluvexStudio/Aether) | هستهٔ تونل، MASQUE/WireGuard | AGPL-3.0 |
| [hufrea/byedpi](https://github.com/hufrea/byedpi) | جعل SNI بدون روت | MIT |
| [heiher/hev-socks5-tunnel](https://github.com/heiher/hev-socks5-tunnel) | پل tun ⇄ SOCKS | MIT |
| [patterniha/SNI-Spoofing](https://github.com/patterniha/SNI-Spoofing) | ایدهٔ اولیه (کد استفاده نشده، فقط ویندوز) | GPL-3.0 |

هیچ‌کدام fork نشده‌اند. Aether به‌صورت پروسهٔ فرزند اجرا می‌شود و کاملاً با
متغیر محیطی کنترل می‌شود، پس هر انتشار جدیدشان بدون تغییر جا می‌افتد.

---

## معماری

```
                    ┌─────────────────────────┐
                    │  RouteBrain             │
                    │  Thompson sampling      │
                    │  روی گوشی، آفلاین        │
                    └───────────┬─────────────┘
                                │ استراتژی را انتخاب می‌کند
┌───────────────────────────────▼──────────────────────────────┐
│  ConnectionController                                        │
│   ۱ IP پایه (مستقیم، قبل از تونل)                            │
│   ۲ انتخاب بازو ──► DesyncEngine  libonvodesync.so  :1080    │
│   ۳ بالا آوردن  ──► NativeCore    libonvocore.so    :1819    │
│   ۴ راستی‌آزمایی ─► ConnectionVerifier — ۶ آزمایش            │
│   ۵ نشتی        ──► DNS + IPv6                               │
│         │                                                    │
│         └── شکست؟ بازو را علامت بزن، بچرخان، دوباره          │
└───────────────────────────────┬──────────────────────────────┘
                                │
                    ┌───────────▼──────────────┐
                    │  OnvoVpnService          │
                    │  tun ⇄ Tun2Socks ⇄ SOCKS │
                    │       libhevsocks5.so    │
                    └──────────────────────────┘
```

### شش آزمایش واقعی

همه **از داخل پروکسی** اجرا می‌شوند، نه شبکهٔ پیش‌فرض. این تفاوت بین
«هندشیک انجام شد» و «بسته‌ها واقعاً رد می‌شوند» است.

| آزمایش | روش |
|---|---|
| تغییر IP | `cdn-cgi/trace` قبل و بعد |
| نشتی DNS | DNS-over-TCP از داخل پروکسی، بررسی ANCOUNT |
| نشتی IPv6 | تلاش عمدی مستقیم روی v6 — جواب داد یعنی نشتی |
| توان عبوری | سه بار دانلود ۱۲۸KB |
| پایداری | مقایسهٔ نمونه‌ها |
| UDP | `UDP ASSOCIATE` روی SOCKS5 |

**اصل مهم:** تونلی که وصل شود ولی ترافیک رد نکند، **شکست** حساب می‌شود و
چرخش استراتژی راه می‌افتد.

### RouteBrain

Contextual multi-armed bandit با نمونه‌گیری تامپسون روی توزیع بتا.
هر بازو یک استراتژی کامل است (پروتکل + عمق اسکن + حالت desync).
زمینه یک سطل درشت است: اپراتور + نوع شبکه + بازهٔ ۶ ساعته.

چرا تامپسون و نه «آخرین چیزی که کار کرد»: سانسور ثابت نیست. استراتژی
مردهٔ امروز ممکن است هفتهٔ بعد کار کند، پس روی همهٔ بازوها کمی احتمال
می‌ماند. یقین قدیمی هم آرام می‌پوسد.

هیچ داده‌ای از گوشی خارج نمی‌شود.

### جعل SNI بدون روت

اندروید `CAP_NET_RAW` نمی‌دهد، پس تزریق بستهٔ خام ممکن نیست و راه دور
زدنی ندارد. ولی همان اثر روی سوکت معمولی به دست می‌آید:

1. `setsockopt(IP_TTL, low)` تا طعمه قبل از سرور بمیرد
2. `vmsplice(SPLICE_F_GIFT)` صفحات ClientHello جعلی را به کرنل می‌دهد
3. همان صفحات در جا با دادهٔ واقعی بازنویسی می‌شوند
4. retransmit کرنل دادهٔ واقعی را با همان sequence می‌فرستد

DPI نام جعلی را ثبت و جریان را رد می‌کند؛ سرور اصلی طعمه را نمی‌بیند.

---

## ⚠️ مشکلات باز — اینها هنوز حل نشده‌اند

کاربر روی گوشی واقعی تست کرده. این فهرست را جدی بگیر.

### ۱. متصل نمی‌شود ‹بحرانی›

علت **احتمالاً** رفع شده (پل tun⇄SOCKS، پایین‌تر توضیح داده شده) ولی
**هرگز روی دستگاه واقعی تأیید نشده**. تا وقتی کاربر تأیید نکرده، فرض کن
هنوز خراب است.

اولین کاری که باید بکنی: build را اجرا کن، APK را به کاربر بده، و بخواه
لاگ کنسول را بفرستد. بدون لاگ حدس نزن.

### ۲. اسپلش اسکرین مثل پروتوتایپ کار نمی‌کند

کاربر می‌گوید «اونجوری که تو پروتوتایپ نشون دادی کار نمیکنه».

آنچه انجام شده: viewport به ۷۲ بزرگ شد تا داخل ناحیهٔ امن ماسک بنشیند،
`windowSplashScreenIconBackgroundColor` کامل حذف شد (باعث بشقاب دور آیکون
در MIUI می‌شد)، و `setKeepOnScreenCondition` با سقف ۱۲۰۰ms اضافه شد.

آنچه ممکن است هنوز مشکل باشد:
- روی MIUI ممکن است `AnimatedVectorDrawable` اصلاً اجرا نشود و فقط فریم
  آخر دیده شود. شیائومی گاهی انیمیشن اسپلش را غیرفعال می‌کند.
- API < 31 نسخهٔ ثابت می‌گیرد، این طبق طراحی است ولی کاربر ممکن است
  انتظار انیمیشن داشته باشد.
- اگر واقعاً روی MIUI کار نکرد، **راه‌حل جایگزین:** یک Activity اسپلش
  سبک با Compose که همان انیمیشن پروتوتایپ را دقیق بازتولید کند. هزینه‌اش
  چند صد میلی‌ثانیه تأخیر است ولی رفتار قابل پیش‌بینی می‌شود.

باید روی دستگاه واقعی دیده شود. در سندباکس قابل تست نیست.

### ۳. تعویض زبان — ‹رفع شد، تأیید نشده›

دو باگ گزارش شد: انگلیسی→فارسی روی LTR گیر می‌کرد، و صفحه یک لحظه سیاه
می‌شد.

علت هر دو یکی بود: `recreate()`. Compose جهت چیدمان را از منابع Activity
نمی‌خواند، از `LocalLayoutDirection` می‌خواند که یک‌بار موقع شروع ترکیب
مقداردهی می‌شود. بازسازی Activity هم پنجره را از نو می‌سازد، که همان
فلاش سیاه است.

رفع: `LocalizedContent` در `LocaleHelper.kt` — زبان مثل state معمولی
Compose رفتار می‌کند و `LocalLayoutDirection` را در همان recomposition
عوض می‌کند. بدون restart، بدون فلاش.

**تأیید نشده روی دستگاه.**

---

## باگی که قبلاً رفع شد — نخوان که دوباره تکرارش کنی

نسخهٔ قبل نصب می‌شد، باز می‌شد، **ولی هرگز وصل نمی‌شد.**

`Tun2Socks` کتابخانهٔ `libhevsocks5.so` را بارگذاری می‌کند، ولی CI فقط
هستهٔ تونل و موتور desync را کامپایل می‌کرد. **پل هیچ‌وقت ساخته نمی‌شد.**

نتیجه: tun ساخته می‌شد، هسته بالا می‌آمد، ولی هیچ بسته‌ای از tun به
پروکسی نمی‌رفت. هر شش آزمایش شکست می‌خورد، مغز درست تشخیص می‌داد که آن
بازو کار نکرد و بعدی را امتحان می‌کرد. کاربر می‌دید «هر دفعه متد عوض
می‌شود ولی وصل نمی‌شود» — **چرخش نشونه بود، نه بیماری.**

سه محافظ اضافه شد:

1. CI حالا `hev-socks5-tunnel` را برای هر سه ABI می‌سازد
2. بعد از assemble، خود APK باز می‌شود و اگر هر کدام از سه کتابخانه نباشد
   **build قرمز می‌شود**
3. اپ قبل از تلاش، وجود فایل‌ها را چک می‌کند و به‌جای هشت بار چرخیدن
   می‌گوید «این نسخه ناقص است، مشکل ساخت است نه شبکه»

---

## ساختار پروژه

```
app/src/main/java/app/onvo/
├── MainActivity.kt              Compose host، اتصال به سرویس، رضایت VPN
├── OnvoApp.kt                   کانال نوتیفیکیشن، اعمال زبان
├── core/
│   ├── ConnectionController.kt  ارکستراسیون، preflight، چرخش استراتژی
│   ├── ConnectionState.kt       UiState، Phase، FailureReason
│   ├── ConnectionVerifier.kt    شش آزمایش، همه از داخل پروکسی
│   ├── DeviceQuirks.kt          شیائومی/هواوی/اوپو/ویوو/سامسونگ
│   └── LocaleHelper.kt          فارسی پیش‌فرض، مقاوم در برابر ریست MIUI
├── ai/RouteBrain.kt             بندیت تامپسون
├── vpn/
│   ├── OnvoVpnService.kt        tun، نوتیفیکیشن، سازگاری MIUI
│   ├── NativeCore.kt            اجرای Aether، پارس خروجی
│   ├── Tun2Socks.kt             پل، با isSupported
│   └── DesyncEngine.kt          ByeDPI، پنج استراتژی
├── diag/LogBus.kt               لاگ + پاک‌سازی خودکار موقع کپی
└── ui/
    ├── components/LiquidPowerButton.kt
    ├── screens/{Home,Console,Settings}Screen.kt
    └── theme/{Theme,OnvoIcons}.kt
```

---

## طراحی — تصمیم‌های قفل‌شده

کاربر اینها را انتخاب کرده. **عوضشان نکن مگر اینکه بخواهد.**

| مورد | انتخاب |
|---|---|
| سبک | مینیمال، الهام از Omega DS + Privity |
| رنگ | تیره فیروزه‌ای `#2FE3C0`، خنثی `#4A5C58`، پس‌زمینه `#080D0C` |
| صفحهٔ اصلی | یک دکمهٔ گرد بزرگ وسط |
| زبان | **فارسی پیش‌فرض** با RTL، انگلیسی اختیاری |
| دکمه | آیکون پاور **کاملاً ثابت**، مایع از پایین با دو موج متقاطع |
| مراحل | حین اتصال دیده، بعد جمع می‌شوند |
| منو | Drawer با هدر وضعیت زنده |
| کنسول | دولایه: سوییچ «جزئیات فنی» ترمینال را با جملات ساده عوض می‌کند |
| لوگو | حلقهٔ O + موج داخلش + نقطهٔ شناور بالای موج |
| اسم | Onvo (از `onda` = موج) |

**چیزهایی که کاربر رد کرد:**
- آدمک و دودل → «ضایعس». المان‌های انتزاعی جایگزین شدند
- تصویرسازی رنگی → حجم APK
- بنر پریمیوم → اپ تجاری نیست

**درخواست صریح کاربر:** «برای افراد مسن و کم‌سواد قابل فهم باشد» — به همین
دلیل کنسول دولایه است و در سطح اول هیچ اصطلاح فنی نیست: «جعل نام سایت»
به‌جای SNI Spoofing، «تأخیر» به‌جای Ping.

---

## محیط ساخت

**سندباکس ۲ گیگابایتی کافی نیست.** کامپایل Kotlin جا می‌شود، ولی dex و R8
مرتب OOM می‌شوند. چهار روش امتحان شد و هیچ‌کدام جواب نداد.

**راه‌حل: ساخت در CI.** فایل `.github/workflows/build.yml` دو مرحله دارد:

1. `native` — Aether (Rust) و ByeDPI و hev-socks5-tunnel (C) با NDK برای
   arm64-v8a، armeabi-v7a، x86_64
2. `apk` — assemble با ۵ گیگ heap، R8، امضا، و **بررسی اینکه هر سه
   کتابخانه واقعاً داخل APK هستند**

برای کامپایل محلی Kotlin:

```bash
bash tools/setup-toolchain.sh
export JAVA_HOME=/home/user/.toolchain/jdk17
export ANDROID_HOME=/home/user/.toolchain/sdk
export GRADLE_USER_HOME=/home/user/.gradlehome
gradle :app:compileDebugKotlin --no-daemon --max-workers=1
```

> `.toolchain` و `.gradlehome` بین سشن‌ها پاک می‌شوند چون از سهمیهٔ
> snapshot بزرگ‌ترند. اسکریپت بازسازی‌شان می‌کند.

---

## آنچه در 0.4.0 اضافه شد

همه کامپایل می‌شوند، هیچ‌کدام روی دستگاه تست نشده‌اند.

| قابلیت | فایل | توضیح |
|---|---|---|
| قطع‌کنندهٔ اضطراری | `OnvoVpnService.kt` | تونل که بیفتد، tun نگه داشته می‌شود و ترافیک دور ریخته می‌شود به‌جای بازگشت به شبکهٔ باز |
| پاک‌سازی مسیر | `SessionHygiene.kt` | هنگام قطع، رکورد quick-reconnect حذف می‌شود تا اتصال بعدی واقعاً رescan کند و جای دیگری برود |
| رادار پینگ | `NetworkRadar.kt` | هر ۳۰ ثانیه از داخل تونل، با ترجمه به «برای چه کاری خوب است» |
| رادار DPI | `NetworkRadar.kt` | چهار سطح: باز / فیلترشده / سخت‌گیرانه / شدید |
| اسکنر SNI | `SniScanner.kt` | ۱۰ نام کاندید را موازی تست می‌کند و سریع‌ترینِ پذیرفته‌شده را برمی‌دارد |
| حالت خودکار/دستی SNI | `SettingsScreen.kt` | خودکار پیش‌فرض است |
| کارت رادار | `RadarCard.kt` | میله‌های سیگنال، شمارش معکوس، نوار سختی شبکه |

**نکتهٔ طراحی مهم:** اسکنر SNI با هندشیک واقعی TLS به یک IP خنثی کار
می‌کند و نام کاندید را در SNI می‌گذارد. گواهی مطابقت نخواهد داشت و این
اشکالی ندارد — سؤال فقط این است که آیا شبکه اجازه داد ClientHello حاوی
آن نام عبور کند یا نه.

---

## کارهای باقی‌مانده

به ترتیب اولویتی که کاربر گفته:

0. **تأیید سه مشکل بالا روی دستگاه واقعی** — قبل از هر کار جدید
1. **بروزرسانی داخل‌برنامه** با هفت لایهٔ محافظ:
   pinning روی گیت‌هاب، چک SHA-256، تطبیق امضا با نسخهٔ نصب‌شده،
   جلوگیری از downgrade، **هرگز نصب خودکار**، لاگ کامل جریان
2. **اجرای واقعی دستورهای کنسول** — چیپ‌ها هستند، اجرا وصل نشده.
   whitelist: `ping`, `dns`, `trace`, `leak-test`, `bench`, `stats`,
   `config show`, `export`. **بدون shell واقعی، بدون `sh -c`** — هر
   دستور یک تابع Kotlin است. این تفاوت ابزار تشخیص و در پشتی است.
3. **انتخاب اپ‌ها** برای عبور یا دور زدن تونل
4. تنظیم استراتژی‌های desync بر اساس رفتار واقعی

---

## سبک کار با این کاربر

- فارسی حرف می‌زند و جواب فارسی می‌خواهد
- **صراحت را ترجیح می‌دهد.** وقتی چیزی کار نمی‌کند، علتش را دقیق بگو.
  وقتی تقصیر توست، بگو تقصیر توست
- ادعای «کار می‌کند» بدون تأیید نکن — همین اشتباه یک بار وقتش را تلف کرد
- تصمیم‌های طراحی را با دلیل توضیح بده، نه فقط اجرا کن
- گوشی **شیائومی** دارد. هر تغییری در سرویس باید با MIUI سازگار بماند
- Play Protect: **دور زدنش نه ممکن است نه لازم.** راه درست امضای ثابت،
  انتشار با چک‌سام، و ثبت در Play Protect Appeals است
