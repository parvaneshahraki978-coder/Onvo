# کارهایی که فقط از دست تو برمی‌آید

ریپو ساخته و کامیت شده. سه کار مانده که هیچ‌کدام را من نمی‌توانم انجام دهم،
چون به حساب گیت‌هاب و کلید خصوصی تو نیاز دارند.

زمان کل: حدود ۱۵ دقیقه.

---

## ۱. آپلود ریپو  ▸ ۳ دقیقه

روی [github.com/new](https://github.com/new) یک مخزن بساز:

- نام: `onvo`
- **Public** (اجباری — لایسنس AGPL سورس‌باز بودن را الزام می‌کند)
- هیچ فایلی اضافه نکن؛ نه README، نه .gitignore، نه LICENSE

بعد در پوشهٔ `onvo`:

```bash
git remote add origin https://github.com/USERNAME/onvo.git
git branch -M main
git push -u origin main
```

به‌محض push، اکشن خودکار اجرا می‌شود و **اولین APK واقعی** ساخته می‌شود:
هستهٔ Aether و ByeDPI با NDK برای سه معماری کراس‌کامپایل می‌شوند و داخل
APK قرار می‌گیرند. حدود ۲۰ تا ۳۰ دقیقه طول می‌کشد چون کامپایل Rust کند است.

خروجی در تب **Actions ← آخرین اجرا ← Artifacts ← onvo-apks**.

> این همان چیزی است که در سندباکس ممکن نبود: کراس‌کامپایل Rust و مرحلهٔ
> dex به چند گیگابایت حافظه نیاز دارد و آنجا فقط ۷۵۰ مگابایت آزاد بود.

---

## ۲. ساخت کلید امضا  ▸ ۵ دقیقه

بدون این، فقط نسخهٔ debug ساخته می‌شود. با این، نسخهٔ release با R8 هم می‌آید.

**کلید را گم نکن.** اگر گم شود، دیگر هرگز نمی‌توانی برای کاربران فعلی
آپدیت منتشر کنی — اندروید نصب APK با امضای متفاوت را رد می‌کند.

```bash
keytool -genkeypair -v \
  -keystore onvo-release.jks \
  -alias onvo \
  -keyalg RSA -keysize 4096 -validity 10000 \
  -storepass 'یک-رمز-قوی' -keypass 'همان-رمز' \
  -dname "CN=Onvo, O=Onvo, C=DE"

base64 -w0 onvo-release.jks > keystore.b64
```

سپس در **Settings ← Secrets and variables ← Actions ← New repository secret**
چهار مورد را اضافه کن:

| نام | مقدار |
|---|---|
| `KEYSTORE_B64` | کل محتوای `keystore.b64` |
| `STORE_PASSWORD` | رمزی که انتخاب کردی |
| `KEY_ALIAS` | `onvo` |
| `KEY_PASSWORD` | همان رمز |

بعد `onvo-release.jks` و `keystore.b64` را **جای امنی خارج از پروژه** نگه دار.
`.gitignore` جلوی کامیت شدنشان را می‌گیرد، ولی پشتیبان با خودت است.

---

## ۳. انتشار نسخه  ▸ ۱ دقیقه

```bash
git tag v0.3.0
git push origin v0.3.0
```

با تگ، علاوه بر ساخت، یک **GitHub Release** با APKها و فایل `SHA256SUMS.txt`
منتشر می‌شود. همان جایی که بروزرسانی داخل‌برنامه بعداً از آن می‌خواند.

---

## دربارهٔ Play Protect

سؤالت را نیمه‌کاره رها کردی، ولی جواب کوتاهش مهم است: **دور زدنش نه ممکن
است نه لازم.** Play Protect بر پایهٔ امضا و رفتار قضاوت می‌کند، نه یک چک
ساده که بشود خاموشش کرد.

راه درست همان چیزی است که در مرحلهٔ ۲ انجام دادی:

- همیشه با **یک کلید ثابت** امضا کن
- از **GitHub Releases** با چک‌سام منتشر کن
- بعد از چند نسخه، اپ را در [Play Protect Appeals](https://support.google.com/googleplay/android-developer/contact/appeal_pha) ثبت کن

Hiddify و v2rayNG دقیقاً همین مسیر را رفتند. هشدار اولیه برای هر APK خارج
از فروشگاه طبیعی است و با افزایش نصب و ثبات امضا کم می‌شود.

---

## وضعیت اسپلش

باگی که دیدی درست بود: سیستم آیکون اسپلش را داخل ماسک لانچر می‌گذارد و
فقط دو‌سوم داخلی تضمین‌شده است. اثر هنری روی شبکهٔ ۴۸ کشیده شده بود ولی
مستقیم در همان اندازه رندر می‌شد، برای همین لبه‌اش به ماسک می‌خورد و
حس «داخل باکس گیر کرده» می‌داد.

سه اصلاح انجام شد:

- viewport به ۷۲ بزرگ شد و اثر با گروه در مرکز نشست، پس حالا کاملاً داخل
  ناحیهٔ امن است
- `windowSplashScreenIconBackgroundColor` **کامل حذف شد**. حتی مقدار
  transparent باعث می‌شود بعضی رام‌ها، مخصوصاً MIUI، یک بشقاب دور آیکون
  بکشند. نبودنش یعنی نشان مستقیم روی پس‌زمینه می‌نشیند.
- `setKeepOnScreenCondition` اضافه شد تا انیمیشن وسط کار قطع نشود، با سقف
  سخت ۱۲۰۰ میلی‌ثانیه که کاربر هرگز الکی منتظر نماند

---

## بعد از اولین build موفق

بگو تا سراغ اینها بروم:

- **بروزرسانی داخل‌برنامه** با هفت لایهٔ محافظ (pinning، چک SHA-256، تطبیق
  امضا با نسخهٔ نصب‌شده، جلوگیری از downgrade، هرگز نصب خودکار)
- **کنسول تشخیصی زنده** — الان چیپ دستورها هست ولی اجرا وصل نشده
- **انتخاب اپ‌ها** برای عبور یا دور زدن تونل
- تنظیم دقیق استراتژی‌های desync بر اساس رفتار واقعی روی اپراتور تو
