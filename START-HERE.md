# Onvo — از اینجا شروع کن

این فایل همه‌چیزی است که برای ادامهٔ کار لازم داری. به تاریخچهٔ گفتگو نیاز نیست.

**ریپوی تو:** github.com/parvaneshahraki978-coder/Onvo

---

## دو فایلی که باید همراهت باشد

| فایل | کارش |
|---|---|
| `Onvo_Setup.ipynb` | نوت‌بوک Colab — همهٔ کارها را خودکار انجام می‌دهد |
| `onvo-source.zip` | سورس کامل اپ، ۵۲ فایل |

هر دو را از گفتگو دانلود کن و به گوشی منتقل کن (تلگرام، درایو، یا کابل).

---

## اجرا، قدم به قدم

### ۱. توکن بساز — ۲ دقیقه

[github.com/settings/tokens/new](https://github.com/settings/tokens/new)

- Note: `onvo-setup`
- Expiration: **7 days**
- تیک‌ها: **`repo`** و **`workflow`** — هر دو لازم است

> بدون `workflow` گیت‌هاب اجازهٔ آپلود پوشهٔ `.github` را نمی‌دهد و هیچ
> APK ساخته نمی‌شود.

توکن را **فقط در نوت‌بوک** وارد کن. هیچ‌جای دیگر ننویس.

### ۲. Colab را باز کن — ۱ دقیقه

[colab.research.google.com](https://colab.research.google.com) →
**Upload** → فایل `Onvo_Setup.ipynb`

### ۳. سلول‌ها را از بالا به پایین اجرا کن — ۳۰ دقیقه

| سلول | کار | زمان |
|---|---|---|
| ۲ | توکن را می‌گیرد و ریپو را چک می‌کند | چند ثانیه |
| ۳ | `onvo-source.zip` را از تو می‌گیرد | ۱ دقیقه |
| ۴ | همه‌چیز را در یک کامیت آپلود می‌کند | ۲ دقیقه |
| ۵ | کلید امضا می‌سازد و secretها را تنظیم می‌کند | ۱ دقیقه |
| ۶ | ساخت را اجرا و دنبال می‌کند | ۲۵ دقیقه |
| ۷ | APK را دانلود می‌کند | ۱ دقیقه |

### ۴. توکن را پاک کن

[github.com/settings/tokens](https://github.com/settings/tokens) →
کنار `onvo-setup` → **Delete**

secretهای ریپو مستقل‌اند و می‌مانند، پس ساخت‌های بعدی کار می‌کنند.

---

## ⚠️ دو فایلی که هرگز نباید گم شوند

سلول ۵ اینها را به تو می‌دهد:

- `onvo-release.jks`
- `onvo-keystore-BACKUP.txt`

**اگر گم شوند، دیگر هرگز نمی‌توانی برای کاربران فعلی آپدیت منتشر کنی.**
اندروید نصب APK با امضای متفاوت را رد می‌کند و همه مجبور می‌شوند اپ را
پاک و دوباره نصب کنند.

جای امنی نگهشان دار — ترجیحاً دو جای مختلف.

---

## بعد از نصب

اپ را باز کن، اجازهٔ VPN را بده.

**اگر گوشیت شیائومی است:** حتماً به **تنظیمات ← سازگاری با گوشی** برو و
هر چهار مورد را فعال کن. بدون اجازهٔ اجرای خودکار، MIUI اتصال را هنگام
خاموش بودن صفحه قطع می‌کند.

**اگر وصل نشد:** منو ← کنسول و لاگ ← سوییچ «نمایش جزئیات فنی» را روشن کن
← **کپی همه**. کلیدها و آی‌پی خودت موقع کپی حذف می‌شوند، پس با خیال راحت
می‌توانی بفرستی.

---

## وضعیت پروژه

| بخش | وضعیت |
|---|---|
| رابط کاربری کامل، فارسی RTL | ✅ |
| دکمهٔ مایع، حلقه‌های ارگانیک | ✅ |
| اسپلش متحرک (باگ باکس اصلاح شد) | ✅ |
| کنسول دولایه با کپی امن لاگ | ✅ |
| سازگاری شیائومی و سایر رام‌ها | ✅ |
| هستهٔ Aether + tun2socks | ✅ کد آماده |
| جعل SNI بدون روت | ✅ کد آماده |
| شش آزمایش واقعی اتصال | ✅ کد آماده |
| مغز انتخاب مسیر (Thompson sampling) | ✅ کد آماده |
| باینری‌های native | ⏳ در همین ساخت درست می‌شوند |

### هنوز مانده

- بروزرسانی داخل‌برنامه با هفت لایهٔ محافظ
- اجرای واقعی دستورهای کنسول (چیپ‌ها هست، اجرا وصل نشده)
- انتخاب اپ‌ها برای عبور یا دور زدن تونل

---

## اگر ساخت قرمز شد

| نشانه | علت | راه‌حل |
|---|---|---|
| هیچ اجرایی شروع نشد | `.github` آپلود نشده | توکن `workflow` نداشته |
| `native` قرمز | ریپوی بالادست تغییر کرده | متن خطا را بفرست |
| `apk` قرمز، `native` سبز | مشکل امضا | سلول ۵ را دوباره اجرا کن |
| فقط debug ساخته شد | secretها نبوده | طبیعی است، APK کار می‌کند |

متن خطا را از تب Actions کپی کن و بفرست.
